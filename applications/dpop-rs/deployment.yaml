apiVersion: apps/v1
kind: Deployment
metadata:
  name: dpop-rs
  namespace: applications
  labels:
    app.kubernetes.io/name: dpop-rs
    owner: platform-team
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dpop-rs
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dpop-rs
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dpop-rs
          # CI (dpop-rs repo) builds this tag; bump it to redeploy a new build.
          image: ghcr.io/kse-kzholtikova/dpop-rs:b532d41e2fefbd7b4048ca1900d12c33495719b2
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: KEYCLOAK_ISSUER_URI
              value: https://keycloak.192.168.50.10.nip.io/realms/api-security
            - name: PUBLIC_BASE_URL
              value: https://dpop-rs.192.168.50.10.nip.io
            # Multipass's bundled qemu mis-virtualizes an ARMv9 instruction that
            # OpenSSL selects at runtime -> SIGILL on the qemu VMs (but not native
            # M4). Disable OpenSSL's ARM-optimized code paths to avoid it.
            - name: OPENSSL_armcap
              value: "0"
            # Root FS is read-only; point gunicorn's control socket at the
            # writable /tmp emptyDir instead of $HOME (/.gunicorn).
            - name: HOME
              value: /tmp
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 32Mi
